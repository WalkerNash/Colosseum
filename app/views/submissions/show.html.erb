
  <%= render 'submission', submission: @submission %>

  <% if @submission.user == current_user %>
  <span><%= link_to "Edit Submission", edit_proposal_submission_path(@submission) %> - 
  <%= link_to 'Destroy', proposal_submission_path, :method => :delete %></span>
<% end %>
